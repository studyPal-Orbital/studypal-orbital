import { FC, ReactNode } from 'react';
import { ConnectedOverlayProps } from 'rdk';
export interface ContextMenuProps extends Omit<ConnectedOverlayProps, 'open'> {
    children: ReactNode;
    content: ReactNode;
    disabled?: boolean;
    autofocus?: boolean;
    autoClose?: boolean;
}
export declare const ContextMenu: FC<ContextMenuProps>;
